image: gradle:6.8.1-jdk11

before_script:
  - apt-get -y update
  - apt-get install -y build-essential

stages:
  - antlrBuild 
  - gradleTest

parser-generate:
  stage: antlrBuild
  script: 
    - make

gradle-test:
  stage: gradleTest 
  script: 
    - make
    - ./gradlew clean test

