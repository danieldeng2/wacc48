image: openjdk:11

before_script:
  - apt-get -y update
  - apt-get install -y make

stages:
  - gradleBuild
  - gradleTest

gradle-build:
  stage: gradleBuild
  script: 
    - ./gradlew jar

gradle-test:
  stage: gradleTest 
  script: 
    - ./gradlew jar
    - ./gradlew clean test

