import org.antlr.runtime.Token
import org.junit.Test

class WACCLexerTest {

    //fun getTokensFromString(string: String): List<Token> {
        // Requires to run ANTLR first to build java classes

//        val byteStream = string.byteInputStream()
//        val charStream = CharStreams.fromStream(byteStream)
//        val lexer = WACCLexer(charStream)
//        val tokenStream = CommonTokenStream(lexer)
//        tokenStream.fill()
//        return tokenStream.tokens

    //}

    @Test
    fun exampleTest() {
//        val tokens = getTokensFromString("1 + 2 + 3")

//        assertEquals(WACCLexer.INT, tokens[0].type)
//        assertEquals(WACCLexer.PLUS, tokens[1].type)
        // ...
    }

    // tests a given string gives a correct list of tokens
}